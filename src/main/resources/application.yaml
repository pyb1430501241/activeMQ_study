server:
  port: 7777

spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
#    pub-sub-domain: false
    pub-sub-domain: true # 订阅

queue: boot-activemq-queue
topic: boot-activemq-topic
